* text=auto eol=lf

# ── GitHub Linguist ─────────────────────────────────────────────────────────
# 语言统计应当反映第一方代码。全仓 37.2 MB 的 .js 里只有约 0.8 MB 是本仓库自己写的，
# 其余全是第三方发行包与生成物。不做标注时 GitHub 显示 JS 54% / TS 42%；
# 标注后是 JS 2.5% / TS 90.3%，后者才是这个仓库的真实构成
# （src 下 .jsx 已为 0，.ts 1,153 个 + .tsx 3,188 个）。
#
# linguist-vendored 与 linguist-generated 都会把文件排除出语言统计；
# 后者还会在 PR diff 里默认折叠——对每个语言约 2 万行的生成翻译包正是想要的效果，
# 合并上游时不必再滚过十几万行机器生成的内容（需要时仍可展开）。

# 第三方发行包
staticfiles/vditordist/**   linguist-vendored
staticfiles/pdf/**          linguist-vendored
staticfiles/tailwindcss.js  linguist-vendored
src/library/**              linguist-vendored

# 混在 src 里的第三方库（都已核对文件头/来源）
# FullCalendar v2.1.0 (c) 2013 Adam Shaw —— 本体 + 46 个语言包
src/pages/calendar/modules/calendarControl/**   linguist-vendored
# CodeMirror 的 mode 与 addon（closebrackets / javascript / matchbrackets / show-hint）
src/pages/widgetConfig/widgetSetting/components/FunctionEditorDialog/Func/lib/**  linguist-vendored
src/components/Mingo/ChatBot/components/Recorder/lib.js  linguist-vendored

# iconfont.cn 生成的字体样式（Project id 2032949）
src/common/mdcss/iconfont/**                    linguist-generated
src/ming-ui/components/IconTabs/iconfont/**     linguist-generated

# scripts/lang.js 生成的翻译包
locale/*/mdTranslation.js   linguist-generated
locale/*/mdTranslation.po   linguist-generated

# ── 刻意【不】排除的，以及为什么 ──────────────────────────────────────────
# scripts/**、CI/**            我们自己的 Node 构建工具链，由 node 直接执行、没有编译步骤。
#                              改成 .ts 会让「构建系统自己得先被编译才能构建」，
#                              凭空增加 bootstrap 复杂度且零收益。它们就该是 JS。
# src/html-templates/**        webpack 的入口模板，靠 cheerio 扫 <script src="webpack[type]?…">
#                              生成 entry。HTML 是它的本质，不存在「改成 ts」。
# staticfiles/staticLanguages.js  第一方代码，html 直接引用。
# DevelopWithAI/examples/**    我们自己维护的示例数据。
# src/pages/integration/svgIcon.js  在 package.json 的 sideEffects 白名单里，需保持可辨识。

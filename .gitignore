# macOS and Windows system files
.DS_Store
Thumbs.db

# Local environment files
.env
.iconfont-session
.claude/settings.local.json
.agents/settings.local.json
/themes/

# Merge conflict and editor backup files
*.orig
*.BASE
*.LOCAL
*.REMOTE
*~HEAD

# Generated files and build output
/build
node_modules
.cache-loader/
*.less.js

# Keep the shared VS Code settings, ignore local workspace files
.vscode/*
!.vscode/settings.json

# Use yarn
package-lock.json

# Yarn 4 (Berry)。白名单式：默认忽略 .yarn/ 下全部内容，只放行需要版本化的几个。
# 本仓用 nodeLinker: node-modules + 全局 cache，所以 .yarn/ 下实际只会出现
# install-state.gz（安装状态缓存，机器相关，不提交）。patches/ 一旦用上必须提交，
# 否则别人装出来的依赖跟你不是一份代码 —— 先写在这里，免得将来打补丁时踩。
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

# Tool output
.codegraph/
.scannerwork
*.llog

# Test files
# 这里曾经有一条 `*.test.js`——由 bb5b797ff(7.4.1) 在删掉 62 个 .test.js 的同一个
# commit 里加入，作用只是让那批测试删干净且再也回不来。它的代价是「静默」：新写的
# 测试不报错、不进 git status、review 里看不见，push 完人间蒸发。已删除。
# 唯一命名规范是 *.spec.js，由 scripts/run-specs.js 启动时【显式报错】强制执行。

# Agent 侧工具（npx skills add ant-design/ant-design-cli 装的），非产品代码。
# 单独列出来是因为本仓的提交惯用 `git add -A`，不挡住会被顺手带进提交。
# 若希望团队共享同一套 skill，把这几行删掉改为提交即可。
.agents/
.claude/
skills-lock.json
